<h1>Listing fresh_books_accounts</h1>

<table>
  <tr>
    <th>Subdomain</th>
    <th>Token</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fresh_books_accounts.each do |fresh_books_account| %>
  <tr>
    <td><%= fresh_books_account.subdomain %></td>
    <td><%= fresh_books_account.token %></td>
    <td><%= link_to 'Show', fresh_books_account %></td>
    <td><%= link_to 'Edit', edit_fresh_books_account_path(fresh_books_account) %></td>
    <td><%= link_to 'Destroy', fresh_books_account, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fresh books account', new_fresh_books_account_path %>

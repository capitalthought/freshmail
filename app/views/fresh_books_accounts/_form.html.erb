<%= form_for(@fresh_books_account) do |f| %>
  <% if @fresh_books_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fresh_books_account.errors.count, "error") %> prohibited this fresh_books_account from being saved:</h2>

      <ul>
      <% @fresh_books_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subdomain %><br />
    <%= f.text_field :subdomain %>
  </div>
  <div class="field">
    <%= f.label :token %><br />
    <%= f.text_field :token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
